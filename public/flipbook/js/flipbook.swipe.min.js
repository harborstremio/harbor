"use strict";
((FLIPBOOK.BookSwipe = class extends FLIPBOOK.Book {
  constructor(i, t, e, s) {
    (super(e, s),
      this.singlePage && (this.view = 1),
      (this.slides = []),
      (this.pagesArr = []),
      (this.leftPage = 0),
      (this.rightPage = 0),
      (this.rotation = 0),
      (this.prevPageEnabled = !1),
      this.setRightIndex(s.rightToLeft ? s.pages.length : 0),
      (this.currentSlide = 0),
      (this.flipping = !1),
      (this.wrapper = t),
      (this.scroller = i),
      this.scroller.classList.remove("book"),
      this.scroller.classList.add("flipbook-carousel-scroller"),
      (this._outerAnimating = !1),
      (this._outerSwipeEnabled = !0),
      (this._outerT = null),
      (this._repositioning = !1),
      (this.zoomDisabled = !1));
    for (let i = 0; i < 3; i++) {
      const i = document.createElement("div");
      i.className = "flipbook-carousel-slide";
      const t = document.createElement("div");
      ((t.className = "slide-inner flipbook-book-shadow"),
        i.appendChild(t),
        this.scroller.appendChild(i),
        (i.pages = []),
        this.slides.push(i));
    }
    for (let i = 0; i < 3; i++)
      ((this.slides[i].iscroll = new IScroll(this.slides[i], {
        zoom: !0,
        scrollX: !0,
        scrollY: !0,
        freeScroll: !0,
        keepInCenterV: !0,
        keepInCenterH: !0,
        preventDefault: !1,
      })),
        this.slides[i].iscroll.on("zoomEnd", function () {
          const i = s.main.zoom;
          ((this.options.eventPassthrough = i > 1 ? "" : "vertical"),
            (this.options.freeScroll = i > 1),
            this.refresh());
        }));
    (this.resizeInnerSlides(),
      s.pages.forEach((i, t) => {
        if (!i.empty) {
          const e = new FLIPBOOK.PageSwipe(this, t, i.src, i.htmlContent);
          (this.pagesArr.push(e), s.loadAllPages && e.load());
        }
      }),
      (this.flipEnabled = !0),
      (this.nextEnabled = !0),
      (this.prevEnabled = !0),
      e.on("enableIScroll", () => this.enableIscroll()),
      e.on("disableIScroll", () => this.disableIscroll()),
      e.on("pageLoaded", function (i) {}),
      this.onResize(!0));
  }
  _setOuterTransition(i, t = 0) {
    this.scroller.style.transition = i ? `transform ${t}ms ease` : "none";
  }
  _setOuterX(i) {
    this.scroller.style.transform = `translate3d(${i}px,0,0)`;
  }
  _slideW() {
    return this.main.wrapperW || this.wrapper.clientWidth || 0;
  }
  _xForSlide(i) {
    return -i * this._slideW();
  }
  _animateToSlide(i, t, e) {
    if (this._outerAnimating) return;
    ((this._outerAnimating = !0),
      (this.flipping = !0),
      (this.wrapper.style.pointerEvents = "none"),
      this._setOuterTransition(!0, t),
      this._setOuterX(this._xForSlide(i)));
    const s = (i) => {
      (i && i.target !== this.scroller) ||
        (this.scroller.removeEventListener("transitionend", s),
        clearTimeout(this._outerT),
        (this._outerT = null),
        (this._outerAnimating = !1),
        (this.flipping = !1),
        (this.wrapper.style.pointerEvents = ""),
        e && e());
    };
    (this.scroller.addEventListener("transitionend", s),
      clearTimeout(this._outerT),
      (this._outerT = setTimeout(() => {
        (this.scroller.removeEventListener("transitionend", s), s());
      }, t + 80)));
  }
  onSwipe(i, t, e, s, h, o) {
    if (!this.enabled) return;
    if (this.zoom > 1) return;
    if (t.startsWith("pinch")) return;
    if (!this.flipEnabled) return;
    if (!this._outerSwipeEnabled) return;
    if (this._outerAnimating || this.flipping) return;
    const l = this._slideW();
    if (
      l &&
      !(
        ("move" === t || "end" === t || "cancel" === t) &&
        Math.abs(s) > Math.abs(e) &&
        Math.abs(s) > 10
      )
    ) {
      if ("start" === t)
        return (
          this.disablePan(),
          this._setOuterTransition(!1),
          void this._setOuterX(this._xForSlide(this.currentSlide))
        );
      if ("move" === t)
        return (
          e < 0 ? this.loadNextSpread() : e > 0 && this.loadPrevSpread(),
          this._setOuterTransition(!1),
          void this._setOuterX(this._xForSlide(this.currentSlide) + e)
        );
      if ("end" !== t && "cancel" !== t);
      else {
        this.enablePan();
        const i = 0.18 * l,
          t = h ? e / h : 0,
          s = 0.8;
        let o = this.currentSlide;
        (e < -i || t < -s) && this.nextEnabled
          ? (o = Math.min(2, this.currentSlide + 1))
          : (e > i || t > s) && this.prevEnabled && (o = Math.max(0, this.currentSlide - 1));
        const n = Math.round(600 * this.options.pageFlipDuration);
        this._animateToSlide(o, n, () => {
          o !== this.currentSlide &&
            (this.singlePage
              ? o > this.currentSlide
                ? this.setRightIndex(this.rightIndex + 1)
                : this.setRightIndex(this.rightIndex - 1)
              : o > this.currentSlide
                ? this.setRightIndex(this.rightIndex + 2)
                : this.setRightIndex(this.rightIndex - 2),
            (this.currentSlide = o),
            (this._repositioning = !0),
            this.updateVisiblePages(),
            (this._repositioning = !1));
        });
      }
    }
  }
  enableIscroll() {
    if (this.iscrollDisabled) {
      if (this.zoom > 1)
        for (let i = 0; i < 3; i++) this.slides[i].iscroll && this.slides[i].iscroll.enable();
      else this._outerSwipeEnabled = !0;
      this.iscrollDisabled = !1;
    }
  }
  disableIscroll() {
    if (!this.iscrollDisabled) {
      if (this.zoom > 1)
        for (let i = 0; i < 3; i++)
          this.slides[i].iscroll &&
            (this.slides[i].iscroll.disable(), (this.slides[i].iscroll.initiated = !1));
      else this._outerSwipeEnabled = !1;
      this.iscrollDisabled = !0;
    }
  }
  goToPage(i, t) {
    if (this.enabled && this.flipEnabled) {
      if (
        (i > 2 * this.numSheets && (i = 2 * this.numSheets),
        (i < 0 || isNaN(i)) && (i = 0),
        (this.singlePage || i % 2 != 0) && i--,
        (isNaN(i) || i < 0) && (i = 0),
        t)
      )
        return (this.setRightIndex(i), void this.updateVisiblePages());
      this.singlePage
        ? i > this.rightIndex
          ? (this.setSlidePages(this.currentSlide + 1, [i]),
            this.setRightIndex(i - 1),
            this.nextPage(t))
          : i < this.rightIndex &&
            (this.setSlidePages(this.currentSlide - 1, [i]),
            this.setRightIndex(i + 1),
            this.prevPage(t))
        : (this.options.rightToLeft && !this.options.backCover && i < 2 && (i = 2),
          i > this.rightIndex
            ? i >= this.pagesArr.length
              ? (this.setSlidePages(2, [i - 1, i]), this.setRightIndex(i - 2), this.goToSlide(2, t))
              : (this.setSlidePages(this.currentSlide + 1, [i - 1, i]),
                this.setRightIndex(i - 2),
                this.nextPage(t))
            : i < this.rightIndex &&
              (0 == i
                ? (this.setRightIndex(i + 2), this.setSlidePages(0, [i]), this.goToSlide(0, t))
                : (this.setRightIndex(i + 2),
                  this.setSlidePages(this.currentSlide - 1, [i - 1, i]),
                  this.prevPage(t))));
    }
  }
  setRightIndex(i) {
    this.rightIndex = i;
  }
  nextPage = function (i) {
    2 != this.currentSlide && (this.goToSlide(this.currentSlide + 1, i), this.loadNextSpread());
  };
  prevPage(i) {
    0 != this.currentSlide && (this.goToSlide(this.currentSlide - 1, i), this.loadPrevSpread());
  }
  enablePrev(i) {
    this.prevEnabled = i;
  }
  enableNext(i) {
    this.nextEnabled = i;
  }
  setSlidePages(i, t) {
    for (var e = this, s = [], h = 0; h < t.length; h++) t[h] && s.push(t[h].index);
    if (
      !(
        this.slides[i].pages &&
        this.slides[i].pages.length > 0 &&
        s.join("") === this.slides[i].pages.join("")
      )
    ) {
      this.clearSlidePages(i);
      var o = this.slides[i].firstChild;
      (t.forEach((t) => {
        if (void 0 !== t) {
          let s;
          ((s = "number" == typeof t ? t : t.index),
            e.pagesArr[s] && (o.appendChild(e.pagesArr[s].wrapper), e.slides[i].pages.push(s)));
        }
      }),
        this.resizeInnerSlides(),
        this.slides[i].iscroll && this.slides[i].iscroll.refresh());
    }
  }
  clearSlidePages(i) {
    ((this.slides[i].firstChild.innerHTML = ""), (this.slides[i].pages = []));
  }
  loadNextSpread() {
    var i = this.rightIndex;
    this.options.rightToLeft && !this.options.backCover && i--;
    var t = this.pagesArr[i + 1];
    if ((t && t.load(), !this.singlePage)) {
      var e = this.pagesArr[i + 2];
      e && e.load();
    }
  }
  loadPrevSpread() {
    var i = this.rightIndex;
    if ((this.options.rightToLeft && !this.options.backCover && i--, this.singlePage)) {
      var t = this.pagesArr[i - 1];
      t && t.load();
    } else {
      var e = this.pagesArr[i - 2];
      e && e.load();
      var s = this.pagesArr[i - 3];
      s && s.load();
    }
  }
  loadVisiblePages() {
    this.options.main;
    var i = this.rightIndex;
    !this.options.rightToLeft || this.options.backCover || this.singlePage || i--;
    var t = this.pagesArr[i],
      e = this.pagesArr[i - 1],
      s = this.pagesArr[i + 1],
      h = this.pagesArr[i + 2],
      o = this.pagesArr[i - 2],
      l = this.pagesArr[i - 3];
    this.singlePage
      ? t
        ? t.load(function () {
            (e && e.load(null, !0), s && s.load(null, !0));
          })
        : e && e.load()
      : e
        ? e.load(function () {
            t
              ? t.load(function () {
                  (o && o.load(null, !0),
                    l && l.load(null, !0),
                    s && s.load(null, !0),
                    h && h.load(null, !0));
                })
              : (o && o.load(null, !0), l && l.load(null, !0));
          })
        : t &&
          t.load(function () {
            (s && s.load(null, !0), h && h.load(null, !0));
          });
  }
  updateVisiblePages() {
    if (this.visiblePagesRightIndex !== this.rightIndex) {
      this.visiblePagesRightIndex = this.rightIndex;
      var i = this.rightIndex;
      ((!this.options.rightToLeft || this.options.backCover || this.singlePage) &&
        this.options.cover) ||
        i--;
      var t = this.pagesArr[i],
        e = this.pagesArr[i - 1],
        s = this.pagesArr[i + 1],
        h = this.pagesArr[i + 2],
        o = this.pagesArr[i - 2],
        l = this.pagesArr[i - 3];
      if (
        (s && s.hideHTML(),
        h && h.hideHTML(),
        o && o.hideHTML(),
        l && l.hideHTML(),
        this.singlePage
          ? (t && t.startHTML(),
            e
              ? s
                ? (this.setSlidePages(1, [t]),
                  e && this.setSlidePages(0, [e]),
                  this.setSlidePages(2, [s]),
                  this.goToSlide(1, !0))
                : (t && this.setSlidePages(2, [t]),
                  e && this.setSlidePages(1, [e]),
                  this.goToSlide(2, !0),
                  this.clearSlidePages(0))
              : (this.setSlidePages(0, [t]),
                s ? this.setSlidePages(1, [s]) : this.clearSlidePages(1),
                this.goToSlide(0, !0),
                this.clearSlidePages(2)),
            e && e.hideHTML())
          : e
            ? (e.startHTML(),
              t
                ? (t.startHTML(),
                  s
                    ? o && (2 != this.rightIndex || this.options.cover)
                      ? (this.setSlidePages(1, [e, t]),
                        this.setSlidePages(0, [l, o]),
                        this.setSlidePages(2, [s, h]),
                        this.goToSlide(1, !0))
                      : (this.setSlidePages(0, [e, t]),
                        this.setSlidePages(1, [s, h]),
                        this.clearSlidePages(2),
                        this.goToSlide(0, !0))
                    : (this.setSlidePages(2, [e, t]),
                      this.setSlidePages(1, [l, o]),
                      this.goToSlide(2, !0),
                      this.clearSlidePages(0)))
                : (this.setSlidePages(2, [e]),
                  this.setSlidePages(1, [l, o]),
                  this.goToSlide(2, !0),
                  this.clearSlidePages(0)))
            : (t && t.startHTML(),
              this.setSlidePages(0, [t]),
              this.setSlidePages(1, [s, h]),
              this.goToSlide(0, !0),
              this.clearSlidePages(2)),
        this.loadVisiblePages(),
        this.singlePage)
      ) {
        const i =
            this.slides[this.currentSlide] &&
            this.slides[this.currentSlide].pages &&
            null != this.slides[this.currentSlide].pages[0]
              ? this.slides[this.currentSlide].pages[0]
              : this.rightIndex,
          t = this.options.rightToLeft ? this.options.numPages - 1 - i : i;
        ((this.flippedleft = t), (this.flippedright = this.options.numPages - t));
      } else
        ((this.flippedleft = (this.rightIndex + (this.rightIndex % 2)) / 2),
          (this.flippedright = this.numSheets - this.flippedleft));
      this.options.main.turnPageComplete();
    }
  }
  loadPage(i) {
    this.pagesArr[i] && this.pagesArr[i].load();
  }
  disable() {
    this.enabled = !1;
  }
  enable() {
    ((this.enabled = !0), this.onResize());
  }
  resize() {}
  updateSinglePage(i) {
    ((this.singlePageView = i), this.onResize(!0));
  }
  onResize(i) {
    var t = this.main.wrapperW,
      e = this.main.wrapperH;
    if (0 != t && 0 != e && (i || this.w !== t || this.h !== e)) {
      ((this.w = t), (this.h = e));
      var s = this.pageWidth,
        h = this.pageHeight,
        o = (2 * this.options.zoomMin * s) / h > t / e,
        l =
          !this.options.singlePageMode &&
          (!this.options.responsiveView ||
            t > this.options.responsiveViewTreshold ||
            !o ||
            t / e >= this.options.responsiveViewRatio);
      void 0 !== this.singlePageView && (l = !this.singlePageView);
      var n,
        r = l ? 2 * s : s,
        a = h;
      ((this.bw = r),
        (this.bh = a),
        (n =
          e / t > a / r ? ((a / r) * t) / this.options.pageHeight : e / this.options.pageHeight));
      for (var d = 0; d < this.slides.length; d++)
        ((this.slides[d].style.width = t + 0 + "px"),
          (this.slides[d].style.height = e + "px"),
          (this.slides[d].style.left = d * t + 0 * d + "px"),
          this.slides[d].iscroll &&
            ((this.slides[d].iscroll.options.zoomMin = this.options.zoomMin * n),
            (this.slides[d].iscroll.options.zoomMax = this.options.zoomMax * n),
            this.slides[d].iscroll.refresh()));
      ((this.scroller.style.width = this.slides.length * (t + 0) + "px"),
        (l && !this.options.singlePageMode) || this.singlePage
          ? l &&
            !this.options.singlePageMode &&
            this.singlePage &&
            (this.rightIndex % 2 != 0 && this.setRightIndex(this.rightIndex + 1),
            (this.singlePage = !1),
            (this.view = 2),
            this.resizeInnerSlides())
          : (this.rightIndex % 2 == 0 &&
              this.rightIndex > 0 &&
              this.setRightIndex(this.rightIndex - 1),
            (this.singlePage = !0),
            (this.view = 1),
            this.resizeInnerSlides()),
        this.zoomTo(this.zoom),
        this._setOuterTransition(!1),
        this._setOuterX(this._xForSlide(this.currentSlide)));
    }
  }
  isFocusedRight() {
    return this.rightIndex % 2 == 0;
  }
  isFocusedLeft() {
    return this.rightIndex % 2 == 1;
  }
  resizeInnerSlides() {
    var i = (this.options.pageHeight * this.pageWidth) / this.pageHeight;
    (90 != this.rotation && 270 != this.rotation) ||
      (i = (this.options.pageHeight * this.pageHeight) / this.pageWidth);
    for (var t = 0; t < 3; t++) {
      let e = this.singlePage ? i : 2 * i;
      ((e = this.slides[t].pages && 1 == this.slides[t].pages.length ? i : 2 * i),
        (this.slides[t].firstChild.style.width = `${e}px`));
    }
  }
  goToSlide(i, t) {
    var e = this.slides[i];
    if ((e.pages && e.pages[0] && this.pagesArr[e.pages[0]].updateHtmlContent(), t))
      return (
        this._setOuterTransition(!1),
        this._setOuterX(this._xForSlide(i)),
        (this.currentSlide = i),
        void this.zoomTo(this.options.zoomMin)
      );
    if (this.flipping) return;
    ((this.flipping = !0), (this.wrapper.style.pointerEvents = "none"));
    const s = Math.round(600 * this.options.pageFlipDuration),
      h = this.currentSlide;
    this._animateToSlide(i, s, () => {
      ((this.currentSlide = i),
        i !== h &&
          (this.singlePage
            ? i > h
              ? this.setRightIndex(this.rightIndex + 1)
              : this.setRightIndex(this.rightIndex - 1)
            : i > h
              ? this.setRightIndex(this.rightIndex + 2)
              : this.setRightIndex(this.rightIndex - 2)),
        (this._repositioning = !0),
        this.updateVisiblePages(),
        (this._repositioning = !1),
        this.zoomTo(this.options.zoomMin),
        (this.flipping = !1),
        (this.wrapper.style.pointerEvents = ""));
    });
  }
  zoomIn(i, t, e) {
    (e && "mousewheel" === e.type) || this.zoomTo(i);
  }
  zoomTo(i, t, e, s) {
    if (this.enabled) {
      ((e = e || 0), (s = s || 0), i > 1 && this.disableFlip());
      var h = this.main,
        o = h.wrapperW,
        l = h.wrapperH;
      if (0 != o && 0 != l) {
        var n = h.bookW,
          r = h.bookH,
          a = h.pageW,
          d = h.pageH,
          p = o / l,
          g = a / d,
          c = Math.min(this.zoom, 1),
          u = Number(this.options.zoomMin),
          m = this;
        (!this.options.singlePageMode &&
        this.options.responsiveView &&
        o <= this.options.responsiveViewTreshold &&
        p < 2 * g &&
        p < this.options.responsiveViewRatio
          ? ((this.view = 1), (this.sc = g > p ? (u * p) / (g * c) : 1), o / l > a / d ? f() : S())
          : this.singlePage && p < 2 * g
            ? ((this.sc = g > p ? (u * p) / (g * c) : 1), o / l > a / d ? f() : S())
            : ((this.view = 2),
              (this.sc = p < 2 * g ? (u * p) / (2 * g * c) : 1),
              o / l >= n / r ? f() : S()),
          (this.zoom = i),
          this.onZoom(i));
      }
    }
    function f() {
      ((m.ratio = l / r), P());
    }
    function S() {
      ((m.ratio = 1 == m.view ? o / a : o / n), P());
    }
    function P() {
      for (var t = 0; t < 3; t++)
        m.slides[t].iscroll &&
          ((m.slides[t].iscroll.options.zoomMin = m.ratio * m.options.zoomMin),
          (m.slides[t].iscroll.options.zoomMax = m.ratio * m.options.zoomMax),
          m.slides[t].iscroll.zoom(m.ratio * i, e, s, 0));
    }
  }
  zoomOut(i) {
    this.zoomTo(i);
  }
  move(i) {
    if (!(this.zoom <= 1))
      for (var t = 0; t < 3; t++) {
        var e = this.slides[t].iscroll;
        if (e) {
          var s = e.x,
            h = e.y,
            o = 20 * this.zoom;
          switch (i) {
            case "left":
              s += o;
              break;
            case "right":
              s -= o;
              break;
            case "up":
              h += o;
              break;
            case "down":
              h -= o;
          }
          (s > 0 && (s = 0),
            s < e.maxScrollX && (s = e.maxScrollX - 0),
            h > 0 && (h = 0),
            h < e.maxScrollY && (h = e.maxScrollY - 0),
            e.scrollTo(s, h, 0));
        }
      }
  }
  onZoom(i) {
    (i > 1 ? (this.disableFlip(), this.enablePan()) : (this.enableFlip(), this.disablePan()),
      this.options.main.onZoom(i));
  }
  rotateLeft() {
    this.rotation = (this.rotation + 360 - 90) % 360;
    for (var i = 0; i < this.pagesArr.length; i++) this.pagesArr[i].setRotation(this.rotation);
    (this.resizeInnerSlides(), this.onResize());
  }
  rotateRight() {
    this.rotation = (this.rotation + 360 + 90) % 360;
    for (var i = 0; i < this.pagesArr.length; i++) this.pagesArr[i].setRotation(this.rotation);
    (this.resizeInnerSlides(), this.onResize());
  }
  onPageUnloaded(i) {
    var t = this.options.rightToLeft ? this.options.numPages - i - 1 : i;
    this.pagesArr[t].unload();
  }
  disableFlip() {
    ((this.flipEnabled = !1), (this._outerSwipeEnabled = !1));
  }
  enableFlip() {
    1 != this.options.numPages
      ? ((this.flipEnabled = !0), (this._outerSwipeEnabled = !0))
      : this.disableFlip();
  }
  enablePan() {
    for (let i = 0; i < 3; i++) this.slides[i].iscroll && this.slides[i].iscroll.enable();
  }
  disablePan() {
    for (let i = 0; i < 3; i++) this.slides[i].iscroll && this.slides[i].iscroll.disable();
  }
}),
  (FLIPBOOK.PageSwipe = class {
    constructor(i, t, e, s) {
      ((this.rotation = 0),
        (this.index = t),
        (this.options = i.options),
        (this.texture = e),
        (this.html = s),
        (this.index = t),
        (this.wrapper = document.createElement("div")),
        this.wrapper.classList.add("flipbook-carousel-page"),
        (this.wrapper.dataset.page = t + 1),
        (this.main = i.main),
        (this.book = i),
        (this.inner = document.createElement("div")),
        this.inner.classList.add("flipbook-carousel-page-inner"),
        this.wrapper.appendChild(this.inner),
        (this.bg = document.createElement("div")),
        this.bg.classList.add("flipbook-carousel-page-bg"),
        this.inner.appendChild(this.bg),
        (this.htmlElement = document.createElement("div")),
        this.htmlElement.classList.add("flipbook-page3-html"),
        (this.htmlElement.style.width =
          (1e3 * this.options.pageWidth) / this.options.pageHeight + "px"),
        (this.htmlElement.style.transform =
          "scale(" + this.options.pageHeight / 1e3 + ") translateZ(0)"),
        this.inner.appendChild(this.htmlElement),
        this.options.doublePage &&
          ((!this.options.rightToLeft && this.index % 2 == 0 && this.index > 0) ||
          (this.options.rightToLeft && this.index % 2 == 1 && this.index > 0)
            ? (this.htmlElement.style.left = "-100%")
            : (this.htmlElement.style.left = "0")),
        (this.preloader = this._createSpinner()),
        this.inner.appendChild(this.preloader),
        this.setSize(this.pw, this.ph));
    }
    _createSpinner() {
      if (this.options.pagePreloader) {
        var i = new Image();
        return (
          (i.src = this.options.pagePreloader), (i.className = "flipbook-page-preloader-image"), i
        );
      }
      var t = Math.round(this.options.pageHeight / 40) + "px",
        e = document.createElement("div");
      e.className = "flipbook-page-preloader";
      var s = document.createElement("div");
      return (
        (s.className = "cssload-speeding-wheel"),
        (s.style.width = t),
        (s.style.height = t),
        e.appendChild(s),
        e
      );
    }
    load(i, t) {
      var e = this.options.pageTextureSize;
      if (this.size >= e) return (t || this.loadHTML(), void (i && i.call(this)));
      this.size = e;
      var s = this,
        h = this.options.rightToLeft ? this.options.numPages - this.index - 1 : this.index,
        o = this.options.pages[h];
      this.options.main.loadPage(h, e, function (h) {
        if ((h = h || {}) && h.image) {
          var l = h.image[e] || h.image;
          (l.classList.add("page-carousel-img"),
            s.index % 2 == 0 &&
              ((o && "left" == o.side) || (o && "right" == o.side)) &&
              (l.clone || ((l.clone = new Image()), (l.clone.src = l.src)), (l = l.clone)),
            s.bg.appendChild(l),
            s.options.rightToLeft
              ? (s.options.doublePage &&
                  s.index < s.options.numPages - 1 &&
                  s.index % 2 == 1 &&
                  (l.style.left = "-100%"),
                s.options.doublePage
                  ? s.index == s.options.numPages - 1 || (0 == s.index && s.options.backCover)
                    ? (l.style.width = "100%")
                    : (l.style.width = "200%")
                  : (l.style.width = "100%"))
              : (s.options.doublePage &&
                  s.index > 0 &&
                  s.index % 2 == 0 &&
                  (l.style.left = "-100%"),
                s.options.doublePage
                  ? 0 == s.index || (s.index == s.options.numPages - 1 && s.options.backCover)
                    ? (l.style.width = "100%")
                    : (l.style.width = "200%")
                  : (l.style.width = "100%")),
            s.preloader && s.preloader.parentNode && s.preloader.remove());
        }
        (s.setRotation(), t || s.loadHTML(), i && i.call(s));
      });
    }
    loadHTML() {
      var i = this,
        t = this.options.rightToLeft ? this.options.numPages - this.index - 1 : this.index;
      this.htmlContent
        ? this.updateHtmlContent()
        : this.options.main.loadPageHTML(t, function (t) {
            ((i.htmlContent = t), i.updateHtmlContent());
          });
    }
    hideHTML() {
      this.htmlContentVisible &&
        ((this.htmlElement.innerHTML = ""),
        (this.htmlContentVisible = !1),
        this.main.trigger("hidepagehtml", { page: this }));
    }
    startHTML() {
      this.book.startPageItems(this.wrapper);
    }
    unload() {
      ((this.pageSize = 0),
        (this.size = 0),
        this.preloader && !this.preloader.parentNode && this.inner.appendChild(this.preloader));
    }
    dispose() {
      this.pageSize && ((this.pageSize = null), (this.bg.innerHTML = ""));
    }
    setSize() {
      var i = this.options.pageWidth,
        t = this.options.pageHeight;
      (0 === this.rotation || 180 === this.rotation
        ? ((this.wrapper.style.width = i + "px"),
          (this.wrapper.style.height = t + "px"),
          (this.pw = i),
          (this.ph = t))
        : ((this.wrapper.style.width = t + "px"),
          (this.wrapper.style.height = i + "px"),
          (this.pw = t),
          (this.ph = i)),
        this.updateHtmlContent());
    }
    setRotation(i) {
      (this.setSize(),
        this.options.doublePage ||
          (void 0 !== i && (this.rotation = i),
          this.img &&
            ((this.img.style.transform = "rotate(" + this.rotation + "deg) translateZ(0)"),
            90 === this.rotation || 270 === this.rotation
              ? ((this.img.style.width = this.wrapper.clientHeight + "px"),
                (this.img.style.height = this.wrapper.clientWidth + "px"))
              : ((this.img.style.width = this.wrapper.clientWidth + "px"),
                (this.img.style.height = this.wrapper.clientHeight + "px")))));
    }
    updateHtmlContent() {
      (this.htmlContent &&
        !this.htmlContentVisible &&
        ((this.htmlContentVisible = !0),
        (this.htmlElement.innerHTML = ""),
        this.htmlElement.appendChild(this.htmlContent),
        this.main.trigger("showpagehtml", { page: this })),
        this.startHTML());
    }
  }));
