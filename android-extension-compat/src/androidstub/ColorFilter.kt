package android.graphics

/** Marker for a colour transform attached to a paint or a drawable. Extensions pass one through
 *  without ever building one, so there is nothing to transform yet. */
open class ColorFilter
