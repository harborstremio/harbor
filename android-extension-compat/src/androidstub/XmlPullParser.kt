package org.xmlpull.v1

// Present only because LayoutInflater.inflate names it in a descriptor the extensions were compiled
// against. It stays an interface so any parser the platform group hands over links against it
// without the two having to agree on a class hierarchy.
interface XmlPullParser
