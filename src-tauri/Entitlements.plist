<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!--
      Harbor's distributable build embeds libmpv and its dependency closure.
      Public CI artifacts are ad-hoc signed and therefore cannot share an Apple
      Team ID with those dylibs. Library validation must therefore be disabled.
      libmpv is also built with LuaJIT, which generates executable code at
      runtime and requires the executable-memory entitlement when Apple's
      hardened runtime is enabled.
    -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>
